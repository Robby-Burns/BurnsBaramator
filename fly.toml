app = "burns-barometer"
primary_region = "sea"

[build]
  dockerfile = "Dockerfile"

[env]
  CLOUD_MODE = "true" # Explicitly set for Fly.io to trigger scheduler

[[vm]]
  memory = "1gb"
  cpu_kind = "shared"
  cpus = 1

# No [[services]] block = Worker Mode (No HTTP checks)
