# Claude Context - BurnsJobBaramator

> Last Updated: February 2026
> Project Phase: Implementation (Step 6)
> Current Sprint: Final Polish & Documentation

---

## 📍 Current State

### What I'm Working On Right Now
- Feature: Documentation & Cleanup
- Files modified: `README.md`, `.dockerignore`, `.gitignore`, `.claude-context.md`
- Status: Ready for Production Run
- Blocker (if any): None

### What Was Built in Last Session
- Created `.dockerignore`
- Updated `README.md` with clear instructions
- Verified `.gitignore` covers all sensitive/generated files
- Confirmed all required docs in `docs/` are present
- Fixed Vercel deployment configuration

---

## 🏗️ Project Structure

### Current Architecture
```
/burns-barometer
  /agents          - Core logic (Scout, Barometer, Mirror, Tribunal, Gatekeeper)
  /db              - Database (SQLite local)
  /utils           - Helpers (LLM Client)
  /docs            - Documentation & Specs
  /tests           - Test Suite
  main.py          - Orchestrator
  config.yaml      - Configuration
```

### Key Files & Their Status
| File | Status | Notes |
|------|--------|-------|
| `main.py` | Complete | Orchestrates full cycle |
| `config.yaml` | Complete | Tunable parameters |
| `README.md` | Updated | Usage instructions |
| `vercel.json` | Fixed | Correct build command |

---

## 🔧 Recent Changes

### Session [Current Date]
**Changed:**
- Finalized documentation
- Added ignore files for safety
- Fixed Vercel deployment issues

**Files Modified:**
- `.dockerignore` (new)
- `README.md` (updated)
- `.gitignore` (updated)
- `vercel.json` (updated)
- `pyproject.toml` (updated)

---

## ⚙️ Configuration & Dependencies

### Current LLM Setup
- Provider: Anthropic (Claude) / OpenAI (swappable)
- Model: Claude 3.5 Sonnet / GPT-4o

### Environment Variables Required
```bash
ANTHROPIC_API_KEY=sk-...       # Required
DATABASE_URL=sqlite:///jobs.db # Default
```

---

## 🎯 Active Features & Status

### In Progress
- [x] Scout agent (scraper) implementation
- [x] Barometer agent (fit analysis)
- [x] Mirror agent (resume generation)
- [x] Tribunal agent (review)
- [x] Gatekeeper agent (approval)
- [x] Testing suite
- [x] Documentation & Cleanup
- [x] Vercel Deployment Config

### Planned
- [ ] Production Run

---

## 🐛 Known Issues & Workarounds

### ISSUE-1: Vercel + SQLite
**Impact:** SQLite is read-only/ephemeral on Vercel.
**Workaround:** Use Docker for local/VPS deployment. For Vercel, must switch `DATABASE_URL` to a hosted Postgres (Neon/Supabase).
**Status:** Documented.

---

## 🚀 Next Steps

### Immediate
1. Run `python main.py` to start finding jobs!

---

**⚠️ REMEMBER TO UPDATE THIS FILE AFTER EVERY BUILD SESSION! ⚠️**
