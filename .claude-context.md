# Claude Context - BurnsJobBaramator

> Last Updated: February 2026
> Project Phase: Implementation (Step 6)
> Current Sprint: Deployment & Bug Fixing

---

## 📍 Current State

### What I'm Working On Right Now
- Feature: Vercel Deployment & API Fixes
- Files modified: `api/index.py`, `vercel.json`, `.bugs_tracker.md`
- Status: Deployment Ready
- Blocker (if any): None

### What Was Built in Last Session
- Fixed `api/index.py` to use SQLAlchemy methods instead of SQLite cursor
- Removed hardcoded DB path in API to support Neon/Postgres
- Updated `vercel.json` to simplify build command
- Documented all bugs in `.bugs_tracker.md`

---

## 🏗️ Project Structure

### Current Architecture
```
/burns-barometer
  /agents          - Core logic (Scout, Barometer, Mirror, Tribunal, Gatekeeper)
  /db              - Database (SQLAlchemy: SQLite/Postgres agnostic)
  /api             - Vercel serverless entry point (FastAPI)
  /utils           - Helpers (LLM Client)
  main.py          - Local orchestrator
  config.yaml      - Configuration
```

### Key Files & Their Status
| File | Status | Notes |
|------|--------|-------|
| `api/index.py` | Fixed | Uses SQLAlchemy, supports Postgres |
| `db/manager.py` | Working | Database agnostic |
| `vercel.json` | Fixed | Zero config for Python 3.11 |

---

## 🔧 Recent Changes

### Session [Current Date]
**Changed:**
- Refactored API to match new DB manager
- Fixed Vercel deployment configuration
- Updated bug tracking

**Files Modified:**
- `api/index.py` (updated)
- `vercel.json` (updated)
- `.bugs_tracker.md` (updated)

---

## ⚙️ Configuration & Dependencies

### Current LLM Setup
- Provider: Anthropic (Claude) / OpenAI (swappable)
- Model: Claude 3.5 Sonnet / GPT-4o

### Environment Variables Required
```bash
ANTHROPIC_API_KEY=sk-...       # Required
DATABASE_URL=sqlite:///jobs.db # Local Default
# DATABASE_URL=postgresql://...  # Production (Neon)
```

---

## 🎯 Active Features & Status

### In Progress
- [x] Scout agent (scraper) implementation
- [x] Barometer agent (fit analysis)
- [x] Mirror agent (resume generation)
- [x] Tribunal agent (review)
- [x] Gatekeeper agent (approval)
- [x] Testing suite
- [x] Vercel Deployment Config
- [x] API Database Integration

### Planned
- [ ] Production Run

---

## 🐛 Known Issues & Workarounds

### ISSUE-1: Vercel + SQLite
**Impact:** SQLite is read-only/ephemeral on Vercel.
**Workaround:** Use Docker for local/VPS deployment. For Vercel, must switch `DATABASE_URL` to a hosted Postgres (Neon/Supabase).
**Status:** Solved via code changes, requires env var setup.

---

## 🚀 Next Steps

### Immediate
1. Push changes to Vercel
2. Verify API endpoint `/jobs/pending` works

---

**⚠️ REMEMBER TO UPDATE THIS FILE AFTER EVERY BUILD SESSION! ⚠️**
