# Claude Context - BurnsJobBaramator

> Last Updated: February 2026
> Project Phase: Deployment (Step 7)
> Current Sprint: Multi-Cloud Deployment Setup

---

## 📍 Current State

### What I'm Working On Right Now
- Feature: Multi-Cloud Deployment (Koyeb, Fly.io, Railway, Vercel)
- Files modified: `README.md`, `docs/DEPLOYMENT.md`, `koyeb.yaml`, `fly.toml`, `.dockerignore`
- Status: Deployment Ready
- Blocker (if any): None

### What Was Built in Last Session
- Added configuration for Koyeb (`koyeb.yaml`) and Fly.io (`fly.toml`)
- Updated `main.py` with a scheduler for 24/7 cloud operation
- Created comprehensive `docs/DEPLOYMENT.md` guide
- Updated `README.md` with deployment info
- Refined `.dockerignore` and `.env.example`

---

## 🏗️ Project Structure

### Current Architecture
```
/burns-barometer
  /agents          - Core logic (Scout, Barometer, Mirror, Tribunal, Gatekeeper)
  /db              - Database (SQLAlchemy: SQLite/Postgres agnostic)
  /api             - Vercel serverless entry point (FastAPI)
  main.py          - Worker entry point (Scheduler/CLI)
  koyeb.yaml       - Koyeb config
  fly.toml         - Fly.io config
  vercel.json      - Vercel config
  Dockerfile       - Docker config
```

### Key Files & Their Status
| File | Status | Notes |
|------|--------|-------|
| `main.py` | Updated | Supports scheduler mode via env var |
| `docs/DEPLOYMENT.md` | New | Full guide for 4 platforms |
| `koyeb.yaml` | New | Ready for Koyeb deploy |
| `fly.toml` | New | Ready for Fly.io deploy |

---

## 🔧 Recent Changes

### Session [Current Date]
**Changed:**
- Added multi-cloud support
- Implemented scheduler logic
- Updated documentation

**Files Modified:**
- `main.py` (updated)
- `docs/DEPLOYMENT.md` (new)
- `koyeb.yaml` (new)
- `fly.toml` (new)
- `README.md` (updated)

---

## ⚙️ Configuration & Dependencies

### Current LLM Setup
- Provider: Anthropic (Claude) / OpenAI (swappable)
- Model: Claude 3.5 Sonnet / GPT-4o

### Environment Variables Required
```bash
ANTHROPIC_API_KEY=sk-...       # Required
DATABASE_URL=postgresql://...  # Required for Cloud (Neon)
RAILWAY_ENVIRONMENT=true       # Triggers scheduler mode (on any cloud)
```

---

## 🎯 Active Features & Status

### In Progress
- [x] Scout agent (scraper) implementation
- [x] Barometer agent (fit analysis)
- [x] Mirror agent (resume generation)
- [x] Tribunal agent (review)
- [x] Gatekeeper agent (approval)
- [x] Testing suite
- [x] Vercel Deployment Config
- [x] Koyeb/Fly/Railway Config
- [x] Scheduler Implementation

### Planned
- [ ] Production Run

---

## 🐛 Known Issues & Workarounds

### ISSUE-1: Vercel + SQLite
**Impact:** SQLite is read-only/ephemeral on Vercel.
**Workaround:** Use Docker for local/VPS deployment. For Vercel, must switch `DATABASE_URL` to a hosted Postgres (Neon/Supabase).
**Status:** Solved via code changes, requires env var setup.

---

## 🚀 Next Steps

### Immediate
1. Push changes to GitHub
2. Connect repo to Koyeb/Railway/Fly
3. Set environment variables in cloud dashboard

---

**⚠️ REMEMBER TO UPDATE THIS FILE AFTER EVERY BUILD SESSION! ⚠️**
