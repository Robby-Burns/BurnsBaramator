[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
markers =
    unit: Unit tests
    integration: Integration tests
    e2e: End-to-end tests
addopts = -v --cov=agents --cov=db --cov=utils
asyncio_mode = auto
